services:
  - type: web
    name: booking-automation
    env: python
   buildCommand: "pip install flask gunicorn playwright gspread oauth2client requests beautifulsoup4 && playwright install --with-deps chromium"
    startCommand: "python3 main.py"
    healthCheckPath: "/health"
